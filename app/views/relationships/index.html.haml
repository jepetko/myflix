%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followed_users.each do |followed_user|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(followed_user.email.downcase)}?s=40")
            = link_to followed_user.full_name, user_path(followed_user)
          %td.extra-padding #{followed_user.queue_items.count}
          %td.extra-padding #{followed_user.followers.count}
          %td.extra-padding
            = link_to 'Unfollow', unfollow_path(id: followed_user.id), method: :delete, class: '.glyphicon.glyphicon-remove'